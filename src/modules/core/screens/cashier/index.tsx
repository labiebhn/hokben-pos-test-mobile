import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import {SafeAreaView} from 'react-native-safe-area-context';

const Cashier = () => {
  return (
    <SafeAreaView>
      <Text>Cashierasdasd</Text>
    </SafeAreaView>
  );
};

export default Cashier;

const styles = StyleSheet.create({});
